/* eslint-disable react/prop-types */
const SearchBar = () => {
    return (
        <form>
            <input type="text" placeholder="pesquise" />
            <button />
        </form>
    );
};

export default SearchBar;